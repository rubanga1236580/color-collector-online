export type ActiveGachaConfig = {
  latest: string;
  revivalA: string;
  revivalB: string;
};

export const ACTIVE_GACHA: ActiveGachaConfig = {
  latest: 'basic',
  revivalA: 'basic',
  revivalB: 'basic',
};
